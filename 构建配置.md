# 构建配置

## 工作流文件

| 文件 | 用途 | 触发条件 |
|------|------|---------|
| `.github/workflows/release.yml` | 上游正式版 Release | 推送 `v*` 标签（如 `v0.1.53`） |
| `.github/workflows/build-carbonfusion.yml` | CarbonFusion 分支构建与 Pre-release | 推送 `carbonfusion-v*` 标签（如 `carbonfusion-v0.1.52`） |

## 为什么用两套标签

上游原版 tag 格式为 `v*`（如 `v0.1.53`），CarbonFusion 自定义 tag 加 `carbonfusion-` 前缀（如 `carbonfusion-v0.1.52`），避免重名冲突。

```
上游：     v0.1.53          → 触发 release.yml（正式 Release）
CarbonFusion：carbonfusion-v0.1.52  → 触发 build-carbonfusion.yml（Pre-release）
```

`release.yml` 监听 `v*`，`carbonfusion-v0.1.52` 以 `carbonfusion-` 开头，不匹配 `v*`，所以不会误触上游的正式发布流程。

## 为什么不改 release.yml 的匹配规则

`release.yml` 是上游的正式发布配置，CarbonFusion 不修改它，避免合并上游代码时产生冲突。

## 构建平台

CarbonFusion 构建 4 个平台（比上游少 2 个，去掉了 Linux ARM64 和 macOS Intel）：

| 平台 | 目标 triple | 二进制名 |
|------|-----------|---------|
| Linux x86_64 | `x86_64-unknown-linux-gnu` | `aioncore` |
| macOS ARM64 | `aarch64-apple-darwin` | `aioncore` |
| Windows x86_64 | `x86_64-pc-windows-msvc` | `aioncore.exe` |
| Windows ARM64 | `aarch64-pc-windows-msvc` | `aioncore.exe` |

## 产物命名

```
aioncore-carbonfusion-v0.1.52-x86_64-unknown-linux-gnu.tar.gz
aioncore-carbonfusion-v0.1.52-aarch64-apple-darwin.tar.gz
aioncore-carbonfusion-v0.1.52-x86_64-pc-windows-msvc.zip
aioncore-carbonfusion-v0.1.52-aarch64-pc-windows-msvc.zip
```

Release 名称和标签名一致，例如 `carbonfusion-v0.1.52`。

## 发布类型

Pre-release（预发布），不会污染正式版本列表。

## 使用方式

```bash
# 打标签并推送，触发构建
git tag carbonfusion-v0.1.52
git push origin carbonfusion-v0.1.52

# 删除标签
git push origin --delete carbonfusion-v0.1.52
git tag -d carbonfusion-v0.1.52
```

## 同步上游代码

同步上游 main 到 carbonfusion 分支时，不影响 CarbonFusion 的自定义 tag，二者完全隔离。